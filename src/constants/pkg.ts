interface PackageJson {
  name: string;
  version: string;
  description: string;
}

const pkg: PackageJson = {
  name: 'envlink',
  version: '0.1.0',
  description: 'A modern CLI tool template built with TypeScript',
};

export default pkg;
